export type AnimalSave = {
    name: string
    color: string
    weightBirth: number
    ringNumber: string
    sex: string
    fatherId: string
    motherId: string
    birthDate: Date
    pastureId: string
    type: string
    observation: string
    isDna: boolean
    isGenotipagem: boolean
}

