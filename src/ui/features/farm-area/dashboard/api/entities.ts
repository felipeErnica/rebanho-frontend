export type FarmInfo = {
    farmId: string
    farmName: string
    pasturesNumber: number
    animalsNumber: number
}

export type PastureInfo = {
    pastureName: string
    bullId?: string
    bullName?: string
    animalsNumber: number
}
